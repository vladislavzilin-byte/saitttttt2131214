IZ HAIR TREND – Integration Pack
--------------------------------

Эти файлы добавляют аккаунт-систему (регистрация, логин, instagram, телефон) в твой текущий проект без замены твоего дизайна страниц.

Что внутри:
- src/context/AuthContext.tsx      (контекст авторизации + localStorage)
- src/components/LoginBox.tsx      (мини-окно входа/аккаунта для главной)
- src/pages/Login.tsx              (полная страница логина)
- src/pages/Register.tsx           (полная страница регистрации)
- src/pages/Shop.tsx               (обновлённая версия с user info для checkout)
- README.txt                       (этот файл)

Шаг 1. Скопируй файлы
---------------------
Скопируй `src/context/AuthContext.tsx`, `src/components/LoginBox.tsx`,
`src/pages/Login.tsx`, `src/pages/Register.tsx` и обновлённый `src/pages/Shop.tsx`
в свой проект (перезаписать Shop.tsx).

Шаг 2. Оберни приложение в AuthProvider
---------------------------------------
В твоём `main.tsx` (или там где у тебя <BrowserRouter>) сделай так:

  import { AuthProvider } from './context/AuthContext'

  <AuthProvider>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </AuthProvider>

Если BrowserRouter уже в App – тогда AuthProvider должен быть снаружи App.

Шаг 3. Добавь Routes
--------------------
В твоём Router добавь маршруты:

  <Route path="/login" element={<Login />} />
  <Route path="/register" element={<Register />} />

(ты уже имеешь /shop, /success, /cancel и т.д.)

Шаг 4. Покажи LoginBox на главной
---------------------------------
Импортируй:
  import LoginBox from './components/LoginBox'

И просто вставь <LoginBox /> в твою главную страницу (там, где у тебя hero / кнопки).

Шаг 5. Backend
--------------
Твой backend (Express server.js) уже принимает checkout POST и использует
`customer_hint`. Ничего менять не надо, просто он начнет получать:
  name / email / instagram / phone
если юзер залогинен.

Готово 🖤
